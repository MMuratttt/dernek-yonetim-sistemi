#!/usr/bin/env sh

# Husky v9+: the helper script sourcing lines are deprecated and can be removed.
# Keep the script minimal; rely on devDependencies already installed locally.

npx --no-install lint-staged
